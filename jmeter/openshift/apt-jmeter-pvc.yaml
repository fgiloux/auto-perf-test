apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
  labels:
    app: jmeter
    name: apt-jmeter
  name: apt-jmeter
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi

